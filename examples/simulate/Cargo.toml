[workspace]

[package]
name = "simulate"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
publish = false

[dependencies]
hdmi-hal = { path = "../.." }
display-types = { path = "../../../display-types" }
